def sub2(x, y): # function of subtraction
    res = x - y #operation it will execute
    print (res) 

sub2 (5, 7)  #same as x = 5, y = 7
# as we have more than one parameter, the order matters. 
#try to always keep the order of parameters, but if you want to change the order, you must specify it: sub(y = 7, x= 5)